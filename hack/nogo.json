{
    "asmdecl": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "assign": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "cgocall": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "composites": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "copylocks": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "errorsas": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "framepointer": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "httpresponse": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "ifaceassert": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "loopclosure": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "lostcancel": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "shift": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "stdmethods": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "stringintconv": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "structtag": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "testinggoroutine": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "tests": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "unmarshal": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "unreachable": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "unsafeptr": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    },
    "unusedresult": {
        "exclude_files": {
            "(external|third_party)/.*": "Enforce only on first-party code"
        }
    }
}
